import {StyleSheet} from 'react-native';

export default styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#A90',
        alignItems: 'center',
        justifyContent: 'center',
    },
});
